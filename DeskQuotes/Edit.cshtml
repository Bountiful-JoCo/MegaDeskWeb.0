@page
@model MegaDeskWeb._0.DeskQuotes.EditModel

@{
    ViewData["Title"] = "Edit a Desk Quote";
}

<h1>Edit a Custom Desk Quote</h1>

<h4>Desk Quote</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="DeskQuote.DeskQuoteID" />
            <div class="form-group">
                <label asp-for="DeskQuote.DeskID" class="control-label"></label>
                <select asp-for="DeskQuote.DeskID" class="form-control" asp-items="ViewBag.DeskID"></select>
                <span asp-validation-for="DeskQuote.DeskID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DeskQuote.QuoteDate" class="control-label"></label>
                <input asp-for="DeskQuote.QuoteDate" class="form-control" />
                <span asp-validation-for="DeskQuote.QuoteDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
